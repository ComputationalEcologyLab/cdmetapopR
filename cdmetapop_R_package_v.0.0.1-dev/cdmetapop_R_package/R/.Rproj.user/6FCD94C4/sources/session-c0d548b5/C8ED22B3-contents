# Nov 17 2024 - All cdmetapop package functions until now, with examples of how to run them
# dominiquealo@gmail.com

install.packages("path/to/yourpackage.tar.gz", repos = NULL, type = "source")
# or
devtools::load_all("yourpath/cdmetapop_R_package")


#load class summary  data
myclass <- read.csv("your_path/summary_classAllTime.csv")
myclasspath <-"your_path/summary_classAllTime.csv"

###################
# age_plus_one_plot
###################
age_plus_one_plot(myclass)

#load population summary  data
mypop <- read.csv("your_path/summary_popAllTime.csv")
mypoppath <- "your_path/summary_popAllTime.csv"

#################
# alleles_by_year
#################
alleles_by_year(mypop) 
# 1.fix function ... it needs to load dyplr


alleles_by_year(mypop, n=20)#  changing bin default

###################
# births
###################
births ("your_path/summary_popAllTime.csv")
# 1. make sure to have installed and loaded ggplot2

###################
# extinct patch
###################
extinct_patch(mypop, year = 3)


###################
# Hets_plot
###################
hets_plot(mypop)

###################
# myy_progeny_plot
###################
myy_progeny_plot(mypoppath)

###################
# pop_count_plot
###################
pop_count_plot(mypop) #fix x-label

###################
# pop_mature_plot
###################
pop_mature_plot(mypoppath)

###################
# pop_patch_count
###################
pop_patch_count(mypop, c(1, 10,25,35,50))

###################
# pop_sex_plot
###################
pop_sex_plot(mypop)

###################
# pop_year_class
###################
pop_year_class(myclasspath, n = 7) 
# 1. fix function - must load reshape2

###################
# separate_column
###################
#load example file
patchvars <- "your_path/PatchEBT415_suppression_YY_Full9_18_2024.csv"
tmp <- separate_column(patchvars, "Genes.Initialize", sep = "|")
View(tmp)

###################
# size_age_class
###################
size_age_class(myclasspath, n = 2)
# 1. check about argument n???? 
# 2. Need to make sure to have reshape2 installed for function 'melt'


###################
# unite_column
###################
tmp2 <- unite_column(tmp, "my_new_genes", ";") 
View(tmp2)
# 1. check code because don't see the new column name... 
# 2. need to install and load 'tidyr'


# New Functions to include????? 
