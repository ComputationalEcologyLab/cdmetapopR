"0","```r

# Calculate basic statistics including k
# Function to summarize statistics
summarize_stats <- function(data, variable) {
  data %>%
    summarise(
      Mean = mean(.data[[variable]], na.rm = TRUE),
      Median = median(.data[[variable]], na.rm = TRUE),
      SD = sd(.data[[variable]], na.rm = TRUE),
      Min = min(.data[[variable]], na.rm = TRUE),
      Max = max(.data[[variable]], na.rm = TRUE)
    )
}

# Basic statistics for ForkLength_mm by Encounter
length_stats <- filtered_yy3 %>%
  group_by(Encounter) %>%
  summarize_stats(\"ForkLength_mm\")

# Basic statistics for Weight_g by Encounter
weight_stats <- filtered_yy3 %>%
  group_by(Encounter) %>%
  summarize_stats(\"Weight_g\")

# Basic statistics for k by Encounter
k_stats <- filtered_yy3 %>%
  group_by(Encounter) %>%
  summarize_stats(\"k\")

# Print tables
# Print tables with extra space
cat(\"## Fork Length Statistics by Encounter\n\")
```"
"1","## Fork Length Statistics by Encounter
"
"0","```r
kable(length_stats, caption = \"Basic Statistics for Fork Length (mm) by Encounter\") %>%
  kable_styling(\"striped\", full_width = F, position = \"left\") %>%
  column_spec(1:ncol(length_stats), width = \"3cm\")  # Adjust the width for columns
```"
