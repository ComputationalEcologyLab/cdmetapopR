% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_popvars.R
\name{.popv_resolve_object_segment}
\alias{.popv_resolve_object_segment}
\title{Resolve one \code{xyfilename} \code{|}-segment to a live PatchVars object or a path}
\usage{
.popv_resolve_object_segment(seg, resolve = TRUE)
}
\arguments{
\item{seg}{A single character segment, or already a \code{PatchVars} object
(passed through unchanged).}

\item{resolve}{If \code{FALSE}, skip the lookup entirely (see
\code{.pv_resolve_class_vars_segment()}'s \code{resolve} argument for the
\code{read_cdmetapop()} rationale, which applies identically here).}
}
\description{
Mirrors PatchVars' \code{.pv_resolve_class_vars_segment()}: if \code{seg} is the
name of a \code{PatchVars} object in \code{.GlobalEnv}, return that object (a live
reference); otherwise return \code{seg} unchanged, to be treated as a literal
path. \code{xyfilename} is the only PopVars field that resolves an object by
name (the matrix fields never do — see \code{.validate_popv_matrix_field()}).
}
\keyword{internal}
