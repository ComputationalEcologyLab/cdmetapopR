% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_pop.R
\name{summary_pop}
\alias{summary_pop}
\title{Plot CDMetaPOP Population Summary Dynamics}
\usage{
summary_pop(
  data,
  type = "N_initial",
  batch_labels = NULL,
  show_mc = TRUE,
  show_ci = TRUE,
  ...
)
}
\arguments{
\item{data}{A dataframe, file path (\code{.csv}, \code{.rds}, \code{.RData}), vector of file
paths, or a directory containing \code{summary_popAllTime.csv} files.}

\item{type}{String specifying the plot type: \code{"N_initial"}, \code{"sex"},
\code{"mature"}, \code{"births"}, \code{"myy_ratio"}, or \code{"patch"}.}

\item{batch_labels}{Optional named character vector used to relabel faceted
source groups. Names should match folder names such as \code{N.out1776184541}
and values should be the labels displayed in the facet strips.}

\item{show_mc}{Logical. If \code{TRUE}, plot individual Monte Carlo trajectories
when multiple source files are supplied. Defaults to \code{TRUE}.}

\item{show_ci}{Logical. If \code{TRUE}, plot the mean and a 95\% confidence band
across Monte Carlo replicates when multiple source files are supplied.
Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to specific plot types. Use
\code{include_yys = TRUE} for \code{"sex"} or \code{"mature"} plots, or \code{years} for
\code{"patch"} plots.}
}
\value{
A ggplot object.
}
\description{
Summarizes and plots data from \code{summary_popAllTime.csv} files. Inputs can be
a data frame, one file path, multiple file paths, or a CDMetaPOP output
directory containing \code{summary_popAllTime.csv} files.
}
\examples{
ex_dir <- system.file("extdata", "Adaptive_Run_08", package = "cdmetapopR")

summary_pop(ex_dir, type = "N_initial")
summary_pop(ex_dir, type = "sex")
summary_pop(ex_dir, type = "mature", include_yys = TRUE)
}
