% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_popvars.R
\name{.validate_popv_matrix_field}
\alias{.validate_popv_matrix_field}
\title{Validate and normalize one matrix-or-path column}
\usage{
.validate_popv_matrix_field(field, values, n)
}
\arguments{
\item{field}{One of \code{.popv_matrix_fields}.}

\item{values}{A single value, or a list/vector of length \code{n}.}

\item{n}{Expected length (the number of batches).}
}
\description{
Matrix fields (\code{.popv_matrix_fields}: the five movement matrices plus
\code{correlation_matrix}/\code{subpopmort_file}) accept, per batch row, one of:
\itemize{
\item a single raw \code{matrix} (a lone value, no \code{|}/\code{~});
\item a filepath string (which may itself carry \code{|}/\code{~} delimiters; stored
verbatim, since CDMetaPOP reads the literal text);
\item the literal \code{"N"}, only for fields in \code{.popv_matrix_allow_N}.
Stored as a list column with ONE item per batch (matrix, path string, or
\code{"N"}) -- not a flat list of items, since a matrix is never combined with
anything (multiple-matrices-over-time/per-sex must be supplied as
filepaths; see Key Design Decision 4 / Parking Lot). A list/vector of
length \code{n} assigns one value per batch; a single matrix/string is
recycled across all batches.
}
}
\keyword{internal}
