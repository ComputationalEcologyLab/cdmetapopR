% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_genetics.R
\name{pairwise_fst_ind}
\alias{pairwise_fst_ind}
\title{Plot Pairwise FST Between Individual Groups}
\usage{
pairwise_fst_ind(
  path,
  year = 0,
  run = 0,
  batch = 0,
  mc = 0,
  species = 0,
  loci = NULL,
  group_col = "PatchID"
)
}
\arguments{
\item{path}{A dataframe, file path, vector of file paths, run directory, or
top-level output directory containing \code{ind##.csv} files.}

\item{year}{Integer. Year/generation to plot for one-year plots. Defaults to
\code{0}.}

\item{run}{Integer. Run index used when \code{path} is a directory. Defaults to
\code{0}.}

\item{batch}{Integer. Batch index used when \code{path} is a directory. Defaults
to \code{0}.}

\item{mc}{Integer. Monte Carlo index used when \code{path} is a directory.
Defaults to \code{0}.}

\item{species}{Integer. Species index used when \code{path} is a directory.
Defaults to \code{0}.}

\item{loci}{Optional vector of loci to include, such as \code{c("L0", "L1")} or
\code{c(0, 1)}. Defaults to all detected loci.}

\item{group_col}{Column used for grouping frequencies. Defaults to
\code{"PatchID"}.}
}
\value{
A ggplot heatmap with pairwise FST data in \code{plot$data}.
}
\description{
Calculates pairwise FST between groups, usually patches, from \code{L#A#}
genotype columns in CDMetaPOP \code{ind##.csv} files. FST is calculated per locus
and then averaged across loci for each pair.
}
\examples{
ex_dir <- system.file("extdata", "Adaptive_Run_08", package = "cdmetapopR")
pairwise_fst_ind(ex_dir, year = 9)
}
