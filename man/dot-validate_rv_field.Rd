% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_runvars.R
\name{.validate_rv_field}
\alias{.validate_rv_field}
\title{Validate and normalize one plain-value RunVars column}
\usage{
.validate_rv_field(field, values, n)
}
\arguments{
\item{field}{Column name (a key in \code{.rv_rules}, i.e. not \code{"Popvars"}).}

\item{values}{Vector of values; recycled to length \code{n} if length 1.}

\item{n}{Expected length (the number of runs already defined).}
}
\description{
Mirrors PopVars' \code{.validate_popv_field()} but only splits on \code{|} (RunVars
has no \code{~}/\code{:} fields). Each \code{|}-separated segment is validated
independently via \code{.rv_check_segment()}, but the whole original string is
stored verbatim as character (the literal text CDMetaPOP expects).
}
\keyword{internal}
