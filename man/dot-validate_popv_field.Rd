% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_popvars.R
\name{.validate_popv_field}
\alias{.validate_popv_field}
\title{Validate and normalize one plain-value PopVars column}
\usage{
.validate_popv_field(field, values, n)
}
\arguments{
\item{field}{Column name (must be a key in \code{.popv_rules}, i.e. not one of
\code{.popv_object_fields}).}

\item{values}{Vector of values to validate; recycled to length \code{n} if it
has length 1.}

\item{n}{Expected length (the number of batches already defined
on the object).}
}
\description{
Mirrors PatchVars' \code{.validate_pv_field()}: splits on '|' first (if
\code{allow_pipe}), then '~' within each '|'-segment (if \code{allow_tilde}),
validating each innermost segment independently via
\code{.popv_check_segment()}, but always storing the whole original string
verbatim as character (the literal text CDMetaPOP expects).
}
\keyword{internal}
