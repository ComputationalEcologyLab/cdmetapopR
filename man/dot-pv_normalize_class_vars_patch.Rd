% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_patchvars.R
\name{.pv_normalize_class_vars_patch}
\alias{.pv_normalize_class_vars_patch}
\title{Normalize one patch's \code{class_vars} entry into a flat list of items}
\usage{
.pv_normalize_class_vars_patch(x, resolve = TRUE)
}
\arguments{
\item{x}{One patch's worth of \code{class_vars} input.}

\item{resolve}{Forwarded to \code{.pv_resolve_class_vars_segment()} -- see
its \code{resolve} argument.}
}
\value{
A flat list, each element a \code{ClassVars} object or character path.
}
\description{
A "item" is either a \code{ClassVars} object or a literal character path.
Handles every shape \code{class_vars}/its per-patch elements can arrive in: a
bare \code{ClassVars} object; a bare (possibly \code{|}-joined) character string,
whose segments are each resolved via \code{.pv_resolve_class_vars_segment()};
or a list/vector of multiple such items (multiple timepoints for this
one patch, to be re-joined with \code{|} at write time) -- recursing so
arbitrarily nested combinations flatten correctly. Uses \code{c()} to
concatenate (not \code{unlist()}), since \code{unlist()} would try to simplify
away the R6 objects living in the list.
}
\keyword{internal}
