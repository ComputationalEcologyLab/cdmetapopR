% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_patchvars.R
\name{.validate_pv_field}
\alias{.validate_pv_field}
\title{Validate and normalize one PatchVars column}
\usage{
.validate_pv_field(field, values, n)
}
\arguments{
\item{field}{snake_case column name (must be in \code{.pv_fields}, excluding
\code{"class_vars"}).}

\item{values}{Vector of values to validate; recycled to length \code{n} if it
has length 1.}

\item{n}{Expected length (the number of patches already defined on the
object).}
}
\value{
A validated vector of length \code{n}: numeric for "numeric"/
"binary01"/"free_numeric" columns whose rule forbids non-numeric
literals, character otherwise.
}
\description{
Internal helper used by every PatchVars active binding except
\code{class_vars}, which is handled separately by \code{.validate_pv_class_vars()}
since it is the one column that can hold R6 objects rather than plain
values, and so cannot be stored as a vector in \code{private$data} the way
every other column is.
}
\keyword{internal}
