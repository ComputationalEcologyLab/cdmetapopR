% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_ind.R
\name{summary_ind}
\alias{summary_ind}
\title{Plot CDMetaPOP Individual File Summaries}
\usage{
summary_ind(
  path,
  type = "age",
  year = 0,
  years = NULL,
  run = 0,
  batch = 0,
  mc = 0,
  species = 0,
  bins = 30
)
}
\arguments{
\item{path}{A dataframe, file path, vector of file paths, run directory, or
top-level output directory containing \code{ind##.csv} files.}

\item{type}{String specifying the plot type: \code{"cdist"}, \code{"hindex"},
\code{"age"}, \code{"size"}, \code{"age_size"}, or \code{"movement"}.}

\item{year}{Integer. Year/generation to plot for one-year plots. Defaults to
\code{0}.}

\item{years}{Integer vector. Years/generations to include for movement
plots. If \code{NULL}, all discovered years are used for \code{"movement"}.}

\item{run}{Integer. Run index used when \code{path} is a directory. Defaults to
\code{0}.}

\item{batch}{Integer. Batch index used when \code{path} is a directory. Defaults
to \code{0}.}

\item{mc}{Integer. Monte Carlo index used when \code{path} is a directory.
Defaults to \code{0}.}

\item{species}{Integer. Species index used when \code{path} is a directory.
Defaults to \code{0}.}

\item{bins}{Integer. Number of bins for continuous histograms. Defaults to
\code{30}.}
}
\value{
A ggplot object.
}
\description{
Plots simple summaries from CDMetaPOP \code{ind##.csv} files. Inputs can be a
data frame, one \code{ind##.csv} file, multiple \code{ind##.csv} files, a single run
folder, or a top-level CDMetaPOP output directory containing run folders.
}
\examples{
ex_dir <- system.file("extdata", "Adaptive_Run_08", package = "cdmetapopR")

summary_ind(ex_dir, type = "age", year = 9)
summary_ind(ex_dir, type = "age_size", year = 9)
summary_ind(ex_dir, type = "movement", years = 0:9, batch = 1, mc = 1)
}
