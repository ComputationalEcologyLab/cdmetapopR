% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_popvars.R
\name{PopVars}
\alias{PopVars}
\title{Create a PopVars object}
\usage{
PopVars(
  n_batches = 4,
  xyfilename = c("patchvars/PatchVars.csv", "patchvars/PatchVars_anadromy.csv",
    "patchvars/PatchVars.csv", "patchvars/PatchVars.csv"),
  mate_cdmat = c("cdmats/Patch7_CdmatrixS1.csv", "cdmats/Patch7_CdmatrixS1.csv",
    "cdmats/Patch7_CdmatrixS1.csv", "cdmats/Patch7_CdmatrixS1.csv"),
  matemoveno = c("6", "6", "4", "6"),
  matemoveparA = c("1.346", "1.346", "1.346", "1.346"),
  matemoveparB = c("202.4846", "202.4846", "202.4846", "202.4846"),
  matemoveparC = c("6", "6", "6", "6"),
  matemovethresh = c("6000", "6000", "6000", "6000"),
  migrateout_cdmat = c("cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv"),
  migratemoveOutno = c("4", "4", "9", "9"),
  migratemoveOutparA = c("0", "0", "0", "0"),
  migratemoveOutparB = c("0", "0", "0", "0"),
  migratemoveOutparC = c("0", "0", "0", "0"),
  migratemoveOutthresh = c("max~max", "max~max", "max~max", "max~max"),
  migrateback_cdmat = c("cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv",
    "cdmats/Patch7_Probmatrix_onewayBarriersXRiverineS1.csv"),
  migratemoveBackno = c("4", "4", "9", "9"),
  migratemoveBackparA = c("0", "0", "0", "0"),
  migratemoveBackparB = c("0", "0", "0", "0"),
  migratemoveBackparC = c("0", "0", "0", "0"),
  migratemoveBackthresh = c("max~max", "max~max", "max~max", "max~max"),
  stray_cdmat = c("cdmats/Patch7_CdmatrixS1.csv", "cdmats/Patch7_CdmatrixS1.csv",
    "cdmats/Patch7_CdmatrixS1.csv", "cdmats/Patch7_CdmatrixS1.csv"),
  StrayBackno = c("1", "1", "4", "2"),
  StrayBackparA = c("0.01", "0.01", "0.01", "0.01"),
  StrayBackparB = c("0.01", "0.01", "0.01", "0.01"),
  StrayBackparC = c("0", "0", "0", "0"),
  StrayBackthresh = c("max", "max", "max", "max"),
  disperseLocal_cdmat = c("cdmats/Patch7_Probmatrix_Dispersal.csv",
    "cdmats/Patch7_Probmatrix_Dispersal.csv", "cdmats/Patch7_CdmatrixS1.csv",
    "cdmats/Patch7_CdmatrixS1.csv"),
  disperseLocalno = c("9", "9", "2", "2"),
  disperseLocalparA = c("0.01", "0.01", "0.01", "0.01"),
  disperseLocalparB = c("0.01", "0.01", "0.01", "0.01"),
  disperseLocalparC = c("0", "0", "0", "0"),
  disperseLocalthresh = c("max", "max", "max", "max"),
  HomeAttempt = c("mortality", "mortality", "mortality", "mortality"),
  sex_chromo = c("2", "2", "2", "2"),
  sexans = c("Y", "Y", "Y", "H"),
  selfans = c("N", "N", "N", "0.5"),
  Freplace = c("Y", "Y", "Y", "Y"),
  Mreplace = c("Y", "Y", "Y", "Y"),
  AssortativeMate_Model = c("1", "1", "1", "1"),
  AssortativeMate_Factor = c("1", "1", "1", "1"),
  mature_default = c("age3|age6|age3", "age3", "size300", "N"),
  mature_eqn_slope = c("0.13~0.06|0.13~0.06|0.13~0.06", "0.0539", "0.13~0.06",
    "0.13~0.06"),
  mature_eqn_int = c("-20.28~-8.09|-20.28~-8.09|-20.28~-8.09", "-6.313", "-20.28~-8.09",
    "-20.28~-8.09"),
  offno = c("2", "2", "2", "2"),
  offans_InheritClassVars = c("random", "random", "random", "random"),
  equalClutchSize = c("N", "N", "N", "N"),
  Egg_Freq_Mean = c("1", "1", "1", "0.5"),
  Egg_Freq_StDev = c("1", "1", "0", "0.1"),
  Egg_Mean_ans = c("linear", "linear", "exp", "exp"),
  Egg_Mean_par1 = c("-445", "-445", "126.07", "126.07"),
  Egg_Mean_par2 = c("3.78", "3.78", "0.0061", "0.0061"),
  Egg_Mortality = c("N", "0.62", "0.62", "0"),
  Egg_Mortality_StDev = c("0", "0", "0.18", "0"),
  Egg_FemaleProb = c("0.5", "0.5", "0.5", "0.5"),
  startGenes = c("0", "0", "0", "0"),
  loci = c("2", "2", "2", "2"),
  alleles = c("2", "2", "2", "2"),
  muterate = c("0", "0", "0", "0"),
  mutationtype = c("random", "random", "random", "random"),
  mtdna = c("N", "N", "N", "N"),
  cdevolveans = c("N", "N", "N", "N"),
  startSelection = c("0", "0", "0", "0"),
  implementSelection = c("Out:Back", "Out:Back", "Eggs", "Back"),
  betaFile_selection = c("N", "N", "N", "N"),
  plasticgeneans = c("N", "N", "N", "N"),
  plasticSignalResponse = c("0", "0", "0", "0"),
  plasticBehavioralResponse = c("0", "0", "0", "0"),
  startPlasticgene = c("0", "0", "0", "0"),
  implementPlasticgene = c("Back:0", "Back:0", "Back", "Back"),
  growth_option = c("temperature", "temperature", "temperature", "temperature"),
  growth_Loo = c("400", "400", "250", "250"),
  growth_R0 = c("0.47", "0.47", "0.57", "0.57"),
  growth_temp_max = c("10.5", "10.5", "12", "12"),
  growth_temp_CV = c("0.33", "0.33", "0.25", "0.25"),
  growth_temp_t0 = c("-0.075", "-0.075", "-0.196", "-0.196"),
  popmodel = c("packing", "anadromy", "logistic_back", "N"),
  popmodel_par1 = c("-0.6821", "-0.6821", "-0.6821", "-0.6821"),
  correlation_matrix = c("N", "N", "N", "N"),
  subpopmort_file = c("N", "N", "N", "N"),
  egg_delay = c("0", "0", "0", "0"),
  egg_add = c("mating", "mating", "mating", "mating"),
  implement_disease = c("N", "N", "N", "N"),
  resolve_xyfilename = TRUE
)
}
\arguments{
\item{n_batches}{Number of independent batches. Defaults to 4,
matching \code{PopVars.csv}. Cannot be changed after construction (use
\code{add_row()} or \code{\link[=add_rows]{add_rows()}} instead).}

\item{xyfilename}{A \code{\link[=PatchVars]{PatchVars()}} object, object-name/path string, for
each batch.}

\item{mate_cdmat, migrateout_cdmat, migrateback_cdmat, stray_cdmat, disperseLocal_cdmat}{A cost-distance/probability \code{matrix} (single value only) or a file path
(\code{|}/\code{~}-delimited for temporal/per-sex), or \code{"N"} for
\code{migrateout_cdmat} (skip the emigration module). See Details.}

\item{matemoveno, migratemoveOutno, migratemoveBackno, StrayBackno, disperseLocalno}{Movement function code, \code{1}-\code{11} (see manual for the function each code
selects).}

\item{matemoveparA, matemoveparB, matemoveparC, migratemoveOutparA, migratemoveOutparB, migratemoveOutparC, migratemoveBackparA, migratemoveBackparB, migratemoveBackparC, StrayBackparA, StrayBackparB, StrayBackparC, disperseLocalparA, disperseLocalparB, disperseLocalparC}{Movement function parameters A/B/C (meaning depends on the
corresponding \verb{*no} function code).}

\item{matemovethresh, migratemoveOutthresh, migratemoveBackthresh, StrayBackthresh, disperseLocalthresh}{Movement threshold: \code{"max"}, \code{"<percent>max"} (e.g. \code{"10max"}), or a
cost-distance value.}

\item{HomeAttempt}{How a migrant that cannot return home is handled:
\code{"mortality"}, \code{"stray_emiPop"}, or \code{"stray_natalPop"}.}

\item{sex_chromo}{Number of sex chromosome combinations: \code{2}, \code{3}, or \code{4}.}

\item{sexans}{Reproduction mode: \code{"Y"} (sexual), \code{"N"} (asexual), or
\code{"H"} (hermaphroditic).}

\item{selfans}{Selfing: \code{"Y"}, \code{"N"}, or a \verb{[0, 1]} probability (for
\code{sexans = "H"}).}

\item{Freplace, Mreplace}{Mating-with-replacement flags, \code{"Y"}/\code{"N"}.}

\item{AssortativeMate_Model}{Assortative mating model: \code{"1"}, \code{"2"},
\code{"3a"}, \code{"3b"}, \code{"4"}, or \code{"5"}.}

\item{AssortativeMate_Factor}{Assortative mating factor, \verb{[1, 1000000]}.}

\item{mature_default}{Default maturation trigger, e.g. \code{"age6"},
\code{"size200"}, or \code{"N"}.}

\item{mature_eqn_slope, mature_eqn_int}{Size-based maturation probability
function parameters.}

\item{offno}{Offspring-count draw choice: \code{1} (uniform), \code{2} (Poisson),
\code{3} (constant), or \code{4} (normal).}

\item{offans_InheritClassVars}{How offspring inherit \code{ClassVars} when
multiple are specified: \code{"random"}, \code{"Hindex"}, or \code{"mother"}.}

\item{equalClutchSize}{\code{"Y"}/\code{"N"}: equal clutch size per mate pair.}

\item{Egg_Freq_Mean, Egg_Freq_StDev}{Egg-laying frequency mean/standard
deviation.}

\item{Egg_Mean_ans}{Size-based fecundity function: \code{"exp"}, \code{"linear"},
or \code{"pow"}.}

\item{Egg_Mean_par1, Egg_Mean_par2}{Parameters for the above function.}

\item{Egg_Mortality, Egg_Mortality_StDev}{Egg mortality \verb{[0, 1]} (or
\code{"N"}) and its standard deviation.}

\item{Egg_FemaleProb}{Probability an egg is female, \verb{[0, 1]}, or
\code{"WrightFisher"}.}

\item{startGenes}{Time unit genetic exchange begins.}

\item{loci, alleles}{Number of loci (>= 2); number of starting alleles per
locus (>= 2), or \code{:}-separated per-locus counts (e.g. \code{"2:5:3"}).}

\item{muterate}{Allele mutation rate, \verb{[0, 1]}.}

\item{mutationtype}{Mutation model: \code{"random"}, \code{"forward"},
\code{"backward"}, \code{"forwardbackward"}, or \code{"forwardAbackwardBrandomN"}.}

\item{mtdna}{\code{"Y"}/\code{"N"}: track a maternal (mtDNA) marker locus.}

\item{cdevolveans}{Selection model/mechanism (open-ended; see manual for
the full grammar, e.g. \code{"N"}, \code{"1"}, \code{"M"}, \code{"Hindex_Gauss_..."}).}

\item{startSelection}{Time unit selection begins.}

\item{implementSelection}{\code{:}-joined timing keyword(s), e.g.
\code{"Out:Back"}.}

\item{betaFile_selection}{Polygenic-selection beta-value file path, or
\code{"N"}.}

\item{plasticgeneans}{Phenotypic plasticity signal/response spec (open-
ended; see manual), or \code{"N"}.}

\item{plasticSignalResponse, plasticBehavioralResponse}{Plasticity signal
trigger value and behavioral response threshold.}

\item{startPlasticgene}{Time unit the plastic process begins.}

\item{implementPlasticgene}{\code{:}-joined timing keyword(s), e.g.
\code{"Back"}.}

\item{growth_option}{Growth function: \code{"N"}, \code{"known"}, \code{"vonB"},
\code{"temperature"}, \code{"temperature_hindex"}, or \code{"bioenergetics"}.}

\item{growth_Loo}{Von Bertalanffy L-infinity, or two \verb{;}-separated
genotype-linked values (\code{"Loo_1;Loo_2"}).}

\item{growth_R0, growth_temp_max, growth_temp_CV, growth_temp_t0}{Remaining
growth function parameters.}

\item{popmodel}{Population growth model: \code{"N"}, \code{"logistic_out"},
\code{"logistic_back"}, \code{"packing"}, \code{"packing_1"}, or \code{"anadromy"}.}

\item{popmodel_par1}{Parameter for the \code{"packing"}/\code{"packing_1"} models.}

\item{correlation_matrix}{Patch-level correlation matrix: a \code{matrix}
(single value), a file path, or \code{"N"}. A matrix field -- see Details.}

\item{subpopmort_file}{Sub-population dispersal mortality matrix: a
\code{matrix} (single value), a file path, or \code{"N"}. A matrix field -- see
Details.}

\item{egg_delay}{Integer >= 0: years between mating and
gestation/emergence.}

\item{egg_add}{\code{"mating"} or \code{"nonmating"} (case-insensitive).}

\item{implement_disease}{Disease module timing: \code{"N"}, \code{"Back"},
\code{"Out"}, or \code{"Both"}.}

\item{resolve_xyfilename}{Whether \code{xyfilename} character entries should
be resolved against \code{PatchVars} objects in \code{.GlobalEnv} by name (see
Details). Defaults to \code{TRUE}; set to \code{FALSE} when every \code{xyfilename}
value is known to already be a literal path (e.g.
\code{read_cdmetapop(..., type = "PopVars")} does this automatically).}
}
\value{
An R6 \code{PopVars} object.
}
\description{
Constructs an R6 object representing a CDMetaPOP \code{PopVars.csv} input
file. Unlike \code{\link[=ClassVars]{ClassVars()}} (rows = age classes) and \code{\link[=PatchVars]{PatchVars()}} (rows =
patches), each row of a \code{PopVars} object is its own independent "batch"
-- CDMetaPOP runs each \code{RunVars} row (a "run") x each \code{PopVars} row (a
"batch") as a separate simulation (see the user manual's run-parameters
section). Despite that, columns are still edited as a whole via \code{$}, e.g.
\code{mypopvars$matemoveno <- c(6, 6, 4, 6)}, for consistency with
\code{ClassVars}/\code{PatchVars}.
}
\details{
Any column argument left unsupplied defaults to the corresponding values
in \code{example_files/popvars/PopVars.csv}, matched to each requested
batch (recycling the row-4 default, with a warning, for rows
beyond what that file defines).

\strong{CDMetaPOP reads \code{PopVars.csv} headers as dictionary keys}, unlike
\code{ClassVars.csv}/\code{PatchVars.csv} (column \emph{order} only) -- so every column
name below is used verbatim as both the data frame's column name and this
function's argument/active-binding name, with no separate human-readable
header text. All manual-documented columns are included, even the
disease-related ones (\code{egg_add}, \code{implement_disease}). \code{cdinfect}/
\code{transmissionprob} were present in the canonical example file but are
deliberately excluded -- confirmed stale leftovers; CDMetaPOP runs fine
without them.

\strong{Delimiters:} most columns support CDMetaPOP's \code{|} (temporal change,
see \code{\link[=PatchVars]{PatchVars()}}'s Details) and/or \code{~} (per-sex, see \code{\link[=ClassVars]{ClassVars()}}'s
Details) mechanisms, per the user manual's \verb{†}/\verb{**} markers respectively.
A few fields use other manual-documented delimiters for a specific
purpose: \code{alleles} and \code{implementSelection}/\code{implementPlasticgene} use
\code{:} (per-locus allele counts; ':'-joined timing keywords, respectively),
and \code{growth_Loo} uses \verb{;} for its two genotype-linked values
(\verb{Loo_1;Loo_2}) -- see \code{.popv_rules}' doc comment in class_popvars.R for
the full per-field delimiter design and known open questions.

\strong{\code{xyfilename} (nested PatchVars reference)} accepts a \code{\link[=PatchVars]{PatchVars()}}
object directly, a character path, or the name of a \code{PatchVars} object in
\code{.GlobalEnv} (see \code{\link[=PatchVars]{PatchVars()}}'s Details for the identical \code{class_vars}
name-resolution mechanism and its caveats).

\strong{Matrix fields} (\code{mate_cdmat}, \code{migrateout_cdmat}, \code{migrateback_cdmat},
\code{stray_cdmat}, \code{disperseLocal_cdmat}, \code{correlation_matrix},
\code{subpopmort_file}) each accept, per batch, ONE of: a single raw R
\code{matrix}; a file path; or (for \code{migrateout_cdmat}, \code{correlation_matrix},
\code{subpopmort_file}) the literal \code{"N"}. \strong{A raw matrix may only be a single
value} -- to vary a matrix over time (the \code{|} cdclimate mechanism) or by
sex (\code{~}), you must supply file \emph{paths}, not matrices (e.g.
\code{pv$mate_cdmat <- "surface0.csv|surface5.csv"}). Supplying a matrix is an
opt-in convenience for the common single-surface case; the field defaults
remain the canonical csv paths, so a no-argument \code{PopVars()} is valid.
When a matrix is supplied, it is serialized to a csv by
\code{\link[=launch_cdmetapop]{launch_cdmetapop()}} (named after your R variable; see the package's file
organization documentation), not by you. If you want control over a
matrix file's location/format, supply a path and write that file
yourself.
}
\examples{
# Default 4-batch object, matching PopVars.csv:
mypopvars <- PopVars()

# 1 batch, defaults taken from PopVars.csv's first row:
mypopvars <- PopVars(n_batches = 1)

# Edit a column in place (one value per batch):
mypopvars$matemoveno <- c(6, 6, 4, 6)

# Add a batch (copies the last row; edit afterward):
mypopvars$add_row()
# or equivalently:
add_rows(mypopvars)
}
