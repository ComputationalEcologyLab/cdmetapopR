% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_runvars.R
\name{.rv_resolve_popvars_segment}
\alias{.rv_resolve_popvars_segment}
\title{Resolve one \code{Popvars} \verb{;}-segment to a live PopVars object or a literal
path}
\usage{
.rv_resolve_popvars_segment(seg, resolve = TRUE)
}
\arguments{
\item{seg}{A single character segment, or already a \code{PopVars} object
(passed through unchanged).}

\item{resolve}{If \code{FALSE}, skip the lookup entirely and return \code{seg}
unchanged -- used when reading an existing RunVars.csv from disk, where
every \code{Popvars} entry is necessarily a literal path.}
}
\description{
Mirrors PopVars' \code{.popv_resolve_object_segment()} (and PatchVars'
\code{.pv_resolve_class_vars_segment()}): if \code{seg} is the name of a \code{PopVars}
object in the global environment, that object is returned (a live
reference); otherwise \code{seg} is returned unchanged, to be treated as a
literal file path. Searches \code{.GlobalEnv} only, for the same reasons
documented in \code{.pv_resolve_class_vars_segment()}.
}
\keyword{internal}
