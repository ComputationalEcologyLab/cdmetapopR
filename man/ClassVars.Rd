% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_classvars.R
\name{ClassVars}
\alias{ClassVars}
\title{Create a ClassVars object}
\usage{
ClassVars(
  ages = 0:5,
  body_size_mean = c(31, 53, 92, 123, 147, 184),
  body_size_std = c(0, 5, 0, 10, 10, 10),
  distribution = c(0.5, 0.25, 0.125, 0.0625, 0.03, 0.015),
  sex_ratio = rep(".50~.50", 6),
  age_mortality_out = rep(0, 6),
  age_mortality_out_stdev = rep(0, 6),
  age_mortality_back = c(0, 0, 0, 0, 0, 1),
  age_mortality_back_stdev = rep(0, 6),
  size_mortality_out = rep(0, 6),
  size_mortality_out_stdev = rep(0, 6),
  size_mortality_back = rep(0, 6),
  size_mortality_back_stdev = rep(0, 6),
  migration_out_prob = c(0, 0.1, 0.3, 0.5, 1, 1),
  migration_back_prob = rep(1, 6),
  straying_prob = rep(0.2, 6),
  dispersal_prob = rep(0.2, 6),
  maturation = c(0, 1, 1, 1, 1, 1),
  fecundity_ind = c(0, 5, 5, 10, 10, 10),
  fecundity_ind_stdev = rep(0, 6),
  fecundity_leslie = c(0, 2.5, 10, 11.7, 11.743, 15),
  fecundity_leslie_stdev = rep(0, 6),
  capture_out_prob = rep("N", 6),
  capture_back_prob = rep("N", 6),
  location = NULL
)
}
\arguments{
\item{ages}{Integer vector of sequential age classes starting at 0
(e.g. \code{0:5}). Determines the number of rows; cannot be changed after
construction (use \code{add_row()} or \code{\link[=add_rows]{add_rows()}} instead).}

\item{body_size_mean, body_size_std}{Mean and standard deviation of body
size (units chosen by the user) at initialization, per age class.}

\item{distribution}{Proportion of the population distributed to each
age class at initialization. Each value in \verb{[0, 1]}; does not need to
sum to 1.}

\item{sex_ratio}{Sex ratio per age class, as a \code{"~"}-separated string of
2-4 values (order: \code{FXX~MXY~MYY~FYY}) that must sum to 1, e.g.
\code{".5~.5"}.}

\item{age_mortality_out, age_mortality_back}{Age-specific mortality
probability \verb{[0, 1]} applied out of/back to the natal patch; or
\code{"N"}; or \code{"~"}-separated per-sex values.}

\item{age_mortality_out_stdev, age_mortality_back_stdev}{Standard
deviation for the above; numeric, \code{"N"}, or \code{"~"}-separated.}

\item{size_mortality_out, size_mortality_back}{Size-specific mortality
probability \verb{[0, 1]}, \code{"N"}, or \code{"~"}-separated per-sex values.}

\item{size_mortality_out_stdev, size_mortality_back_stdev}{Standard
deviation for the above; numeric, \code{"N"}, or \code{"~"}-separated.}

\item{migration_out_prob, migration_back_prob, straying_prob, dispersal_prob}{Movement probabilities \verb{[0, 1]}, \code{"N"}, or \code{"~"}-separated per-sex
values.}

\item{maturation}{Probability \verb{[0, 1]} of being/becoming reproductively
mature; or \code{"N"}; or \code{"~"}-separated per-sex values.}

\item{fecundity_ind, fecundity_ind_stdev}{Mean and standard deviation of
individual-based fecundity (litter/egg count) per age class.}

\item{fecundity_leslie, fecundity_leslie_stdev}{Mean and standard
deviation of Leslie-matrix fecundity per age class.}

\item{capture_out_prob, capture_back_prob}{Capture/detection probability
\verb{[0, 1]}, \code{"N"}, or \code{"~"}-separated per-sex values.}

\item{location}{Optional file path where this object should be written
by default when \code{\link[=write_cdmetapop]{write_cdmetapop()}} is called without a \code{path}.}
}
\value{
An R6 \code{ClassVars} object.
}
\description{
Constructs an R6 object representing a CDMetaPOP \code{ClassVars.csv} input
file: one row per age/size class, with one column per class-level
parameter (mortality, migration, fecundity, etc.). Columns are edited as
a whole after construction via \code{$}, e.g.
\code{myclassvars$age_mortality_out <- c(0, 0, 0.5)}.
}
\details{
Any column argument left as \code{NULL} defaults to the corresponding values
in \code{example_files/classvars/ClassVarsAS1.csv}, matched to each requested
age class (recycling the age-5 default, with a warning, for age classes
beyond what that file defines).
}
\examples{
# Default 6-age-class object, matching ClassVarsAS1.csv:
myclassvars <- ClassVars()

# 3 age classes, defaults taken from ClassVarsAS1.csv's first 3 rows:
myclassvars <- ClassVars(ages = c(0, 1, 2))

# Edit a column in place:
myclassvars$age_mortality_out <- c(0, 0, 0.5)

# Add an age class (copies the last row; edit afterward):
myclassvars$add_row()
# or equivalently:
add_rows(myclassvars)
}
