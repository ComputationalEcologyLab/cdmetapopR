% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_runvars.R
\name{.validate_rv_popvars}
\alias{.validate_rv_popvars}
\title{Validate and normalize the \code{Popvars} column}
\usage{
.validate_rv_popvars(values, n, resolve = TRUE)
}
\arguments{
\item{values}{A single value, or a list/vector of length \code{n}, each
element one run's worth of \code{Popvars} input.}

\item{n}{Expected length (the number of runs).}

\item{resolve}{Forwarded to \code{.rv_normalize_popvars_row()} -- \code{FALSE}
disables \code{.GlobalEnv} name resolution (used when reading from disk).}
}
\description{
Stored as a list column (R6 objects cannot live in an atomic vector).
Each element is itself a flat list of one or more items (multiple
species per run), normalized by \code{.rv_normalize_popvars_row()}. Mirrors
PopVars' \code{.validate_popv_object_field()}.
}
\keyword{internal}
