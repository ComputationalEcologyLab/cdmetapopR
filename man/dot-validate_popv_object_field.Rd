% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_popvars.R
\name{.validate_popv_object_field}
\alias{.validate_popv_object_field}
\title{Validate and normalize the \code{xyfilename} object-or-path column}
\usage{
.validate_popv_object_field(field, values, n, resolve = TRUE)
}
\arguments{
\item{values}{A single value, or a list/vector of length \code{n}, each
element one batch's worth of input.}

\item{n}{Expected length (the number of batches).}

\item{resolve}{Forwarded to \code{.popv_normalize_object_row()} -- \code{FALSE}
disables \code{.GlobalEnv} name resolution (used by \code{read_cdmetapop()},
where a value read from an existing csv is always a literal path).}
}
\description{
Stored as a list column (R6 objects cannot live in an atomic vector).
Each element is itself a flat list of one or more items, normalized by
\code{.popv_normalize_object_row()} -- mirrors PatchVars' \code{.validate_pv_class_vars()}.
}
\keyword{internal}
