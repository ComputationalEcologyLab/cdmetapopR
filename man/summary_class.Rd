% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_class.R
\name{summary_class}
\alias{summary_class}
\title{Plot CDMetaPOP Class Summary Dynamics}
\usage{
summary_class(
  data,
  type = "age_class",
  batch_labels = NULL,
  show_mc = TRUE,
  show_ci = TRUE,
  run = 0,
  batch = 0,
  mc = 0,
  species = 0,
  ...
)
}
\arguments{
\item{data}{A dataframe, file path (\code{.csv}, \code{.rds}, \code{.RData}), vector of file
paths, or a directory containing \code{summary_classAllTime.csv} files.}

\item{type}{String specifying the plot type: \code{"age_class"} or
\code{"age_plus_one"}.}

\item{batch_labels}{Optional named character vector used to relabel faceted
source groups. Names should match folder names such as \code{N.out1776184541}
and values should be the labels displayed in the facet strips.}

\item{show_mc}{Logical. If \code{TRUE}, plot individual Monte Carlo trajectories
when multiple source files are supplied for line-based plots. Defaults to
\code{TRUE}.}

\item{show_ci}{Logical. If \code{TRUE}, plot the mean and a 95\% confidence band
across Monte Carlo replicates when multiple source files are supplied for
line-based plots. Defaults to \code{TRUE}.}

\item{run}{Integer run index used when \code{data} is a directory. Defaults to
\code{0}. Use \code{"all"} to include all runs.}

\item{batch}{Integer batch index used when \code{data} is a directory. Defaults
to \code{0}. Use \code{"all"} to include all batches.}

\item{mc}{Integer Monte Carlo index used when \code{data} is a directory.
Defaults to \code{0}. Use \code{"all"} to include all Monte Carlo replicates.}

\item{species}{Integer species index used when \code{data} is a directory.
Defaults to \code{0}. Use \code{"all"} to include all species.}

\item{...}{Additional arguments passed to specific plot types. Use \code{n} to
choose the year interval for \code{"age_class"} plots.}
}
\value{
A ggplot object.
}
\description{
Summarizes and plots data from \code{summary_classAllTime.csv} files. Inputs can
be a data frame, one file path, multiple file paths, or a CDMetaPOP output
directory containing \code{summary_classAllTime.csv} files.
}
\examples{
ex_dir <- system.file("extdata", "Adaptive_Run_08", package = "cdmetapopR")

summary_class(ex_dir, type = "age_class", n = 10)
summary_class(ex_dir, type = "age_plus_one")
}
