% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_dataframe.R
\name{summary_dataframe}
\alias{summary_dataframe}
\title{Return CDMetaPOP Output Data as Data Frames}
\usage{
summary_dataframe(
  data,
  type = c("pop", "class", "disease", "ind"),
  run = 0,
  batch = 0,
  mc = 0,
  species = 0,
  years = NULL,
  file_type = "ind",
  patches = "all",
  state_names = NULL,
  cumulative_states = NULL,
  state_column = "States_SecondUpdate",
  disease_format = c("long", "wide"),
  summary_format = c("long", "wide")
)
}
\arguments{
\item{data}{A data frame, file path, vector of file paths, run directory, or
top-level CDMetaPOP output directory.}

\item{type}{Character. Which file type to read: \code{"pop"} for
\code{summary_popAllTime.csv}, \code{"class"} for \code{summary_classAllTime.csv},
\code{"disease"} for \code{summary_popAllTime_DiseaseStates.csv}, or \code{"ind"} for
\code{ind##.csv} / \code{ind##_Sample.csv} files.}

\item{run}{Integer run index used when \code{data} is a directory. Defaults to
\code{0}. Use \code{"all"} to include all runs.}

\item{batch}{Integer batch index used when \code{data} is a directory. Defaults
to \code{0}. Use \code{"all"} to include all batches.}

\item{mc}{Integer Monte Carlo index used when \code{data} is a directory.
Defaults to \code{0}. Use \code{"all"} to include all Monte Carlo replicates.}

\item{species}{Integer species index used when \code{data} is a directory.
Defaults to \code{0}. Use \code{"all"} to include all species.}

\item{years}{Optional integer vector of years/generations to include for
\code{type = "ind"}.}

\item{file_type}{Character. Which individual file type to read for
\code{type = "ind"}. Use \code{"ind"} for \code{ind##.csv} files or \code{"ind_Sample"} for
\code{ind##_Sample.csv} files. Defaults to \code{"ind"}.}

\item{patches}{Patch IDs to include for \code{type = "ind"}. Use \code{"all"} to
include all patches, a single patch ID, or a vector/range of patch IDs.
Defaults to \code{"all"}.}

\item{state_names}{Character vector used to name disease states for
\code{type = "disease"}. If \code{NULL}, states are named numerically.}

\item{cumulative_states}{Character vector of disease state names to
calculate as running totals for \code{type = "disease"}.}

\item{state_column}{Character. Disease-state column to parse for
\code{type = "disease"}. Defaults to \code{"States_SecondUpdate"}.}

\item{disease_format}{Character. Use \code{"long"} to return one row per
year/source/state, or \code{"wide"} to return one column per state. Defaults to
\code{"long"}.}

\item{summary_format}{Character. Use \code{"long"} to split pipe-delimited
\code{summary_popAllTime.csv} and \code{summary_classAllTime.csv} columns into one
row per patch or class while keeping metrics in separate columns. Use
\code{"wide"} to return the raw summary columns with metadata. Defaults to
\code{"long"}.}
}
\value{
A data frame with source metadata columns. For \code{type = "pop"} and
\code{type = "class"}, the default long format includes \code{PatchID} or \code{ClassID}
indexing values split from pipe-delimited metric columns. For
\code{type = "disease"}, the default long format includes \code{State} and \code{Count}
columns.
}
\description{
Reads CDMetaPOP population, class, disease-state, or individual output files
and returns data frames for custom plotting or downstream summaries.
}
\examples{
ex_dir <- system.file("extdata", "Example_dat", package = "cdmetapopR")

pop_df <- summary_dataframe(ex_dir, type = "pop")
class_df <- summary_dataframe(ex_dir, type = "class")
disease_df <- summary_dataframe(ex_dir, type = "disease")
ind_df <- summary_dataframe(ex_dir, type = "ind", years = 9)

all_mc_pop_df <- summary_dataframe(ex_dir, type = "pop", mc = "all")
}
